# Detection-only catalog entry.
# Generated by scripts/gen-detection-catalog.py — do not edit by hand;
# edit the script and re-run.
apiVersion: cilock.detection/v0.1
name: vexctl
detection_only: true

description: vexctl — OpenVEX statement generation.

category: [vex-consume]

upstream:
  name: vexctl
  source: "https://github.com/openvex/vexctl"
  license: Apache-2.0
  vendor: OpenVEX / Chainguard

emits_formats: [vex]

pre:
  match:
    argv_prefix: [vexctl]

llm_hints:
  on_match: vexctl invocation observed. OpenVEX document captured by the vex attestor.
