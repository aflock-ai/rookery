# Detection-only catalog entry.
# Generated by scripts/gen-detection-catalog.py — do not edit by hand;
# edit the script and re-run.
apiVersion: cilock.detection/v0.1
name: melange
detection_only: true

description: melange — builds apk packages from declarative YAML pipelines (Chainguard); emits an SBOM per package.

category: [build]

upstream:
  name: melange
  source: "https://github.com/chainguard-dev/melange"
  license: Apache-2.0
  vendor: Chainguard

emits_formats: [sbom]

recommended_trace: full

pre:
  match:
    argv_prefix: [melange, build]

llm_hints:
  on_match: melange build observed. The sbom attestor captures the SPDX SBOM melange generates for each apk.
