# Detection-only catalog entry.
# Generated by scripts/gen-detection-catalog.py — do not edit by hand;
# edit the script and re-run.
apiVersion: cilock.detection/v0.1
name: cargo-audit
detection_only: true

description: cargo-audit — Rust dependency vulnerability scanner.

category: [vulnerability-scan]

upstream:
  name: cargo-audit
  source: "https://github.com/rustsec/rustsec"
  license: Apache-2.0 OR MIT
  vendor: RustSec / Rust Foundation

pre:
  match:
    argv_prefix: [cargo-audit]

llm_hints:
  on_match: cargo audit observed. JSON output (--json) captured.
