# Detection-only catalog entry.
# Generated by scripts/gen-detection-catalog.py — do not edit by hand;
# edit the script and re-run.
apiVersion: cilock.detection/v0.1
name: zap
detection_only: true

description: OWASP ZAP — dynamic application security testing (DAST) via the automation framework; commonly run from the zaproxy container image.

category: [vulnerability-scan]

upstream:
  name: OWASP ZAP
  source: "https://github.com/zaproxy/zaproxy"
  license: Apache-2.0
  vendor: OWASP / Checkmarx

emits_formats: [sarif]

pre:
  match:
    argv_prefix: [zap-baseline.py]

llm_hints:
  on_match: "OWASP ZAP scan observed (zap-baseline.py / automation framework). The SARIF report is captured by the sarif attestor; when run via `docker run`, the literal docker argv is recorded by command-run."
