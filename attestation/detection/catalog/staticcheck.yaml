# Detection-only catalog entry.
# Generated by scripts/gen-detection-catalog.py — do not edit by hand;
# edit the script and re-run.
apiVersion: cilock.detection/v0.1
name: staticcheck
detection_only: true

description: staticcheck — Go static analyzer (Honnef.co/go/tools).

category: [lint]

upstream:
  name: staticcheck
  source: "https://github.com/dominikh/go-tools"
  license: MIT
  vendor: Dominik Honnef

emits_formats: [sarif]

pre:
  match:
    argv_prefix: [staticcheck]

llm_hints:
  on_match: staticcheck invocation observed. Findings captured via stdout / SARIF (-f sarif).
