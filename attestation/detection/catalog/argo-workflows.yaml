# Detection-only catalog entry.
# Generated by scripts/gen-detection-catalog.py — do not edit by hand;
# edit the script and re-run.
apiVersion: cilock.detection/v0.1
name: argo-workflows
detection_only: true

description: Argo Workflows — Kubernetes-native workflow engine (CNCF).

category: [build]

upstream:
  name: Argo Workflows
  source: "https://github.com/argoproj/argo-workflows"
  license: Apache-2.0
  vendor: CNCF / Argoproj

pre:
  match:
    env_set: ARGO_TEMPLATE

llm_hints:
  on_match: "Running inside an Argo Workflow step. Workflow metadata available via ARGO_* env vars."
