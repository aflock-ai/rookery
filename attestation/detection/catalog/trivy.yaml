# Detection-only catalog entry.
# Generated by scripts/gen-detection-catalog.py — do not edit by hand;
# edit the script and re-run.
apiVersion: cilock.detection/v0.1
name: trivy
detection_only: true

description: "Trivy — Aqua Security's all-in-one scanner (vulnerabilities, IaC misconfig, secrets, licenses) for containers, filesystems, and repos."

category: [vulnerability-scan]

upstream:
  name: Trivy
  source: "https://github.com/aquasecurity/trivy"
  license: Apache-2.0
  vendor: Aqua Security

emits_formats: [sarif]

pre:
  match:
    argv_prefix: [trivy]

llm_hints:
  on_match: "Trivy scan observed. --format sarif --output <file>.sarif is captured by the sarif attestor."
