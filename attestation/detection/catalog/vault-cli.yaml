# Detection-only catalog entry.
# Generated by scripts/gen-detection-catalog.py — do not edit by hand;
# edit the script and re-run.
apiVersion: cilock.detection/v0.1
name: vault-cli
detection_only: true

description: HashiCorp Vault CLI — secret distribution / dynamic credentials.

category: [runtime]

upstream:
  name: HashiCorp Vault
  source: "https://github.com/hashicorp/vault"
  license: BUSL-1.1 (since 2023) / MPL-2.0 (pre-1.14)
  vendor: HashiCorp / IBM

pre:
  match:
    argv_prefix: [vault]

llm_hints:
  on_match: Vault CLI invocation observed. Secret/credential flow into the build environment.
