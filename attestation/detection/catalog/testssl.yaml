# Detection-only catalog entry.
# Generated by scripts/gen-detection-catalog.py — do not edit by hand;
# edit the script and re-run.
apiVersion: cilock.detection/v0.1
name: testssl
detection_only: true

description: "testssl.sh — TLS connection scanner: protocol negotiation, cipher matrix, named TLS vulnerabilities, and FIPS 140-2/140-3 compliance findings."

category: [vulnerability-scan]

upstream:
  name: testssl.sh
  source: "https://github.com/testssl/testssl.sh"
  license: GPL-2.0
  vendor: Dirk Wetter / testssl.sh maintainers

emits_formats: [sarif]

pre:
  match:
    argv_prefix: [testssl.sh]

llm_hints:
  on_match: testssl.sh scan observed. --jsonfile-pretty output is captured (parsed by the sarif attestor).
