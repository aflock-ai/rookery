# Detection-only catalog entry.
# Generated by scripts/gen-detection-catalog.py — do not edit by hand;
# edit the script and re-run.
apiVersion: cilock.detection/v0.1
name: semgrep
detection_only: true

description: Semgrep — open-source static analysis with SARIF / JSON output.

category: [vulnerability-scan]

upstream:
  name: Semgrep
  source: "https://github.com/semgrep/semgrep"
  license: LGPL-2.1-only
  vendor: Semgrep Inc.

emits_formats: [sarif]

exits_nonzero_on_findings: true

pre:
  match:
    argv_prefix: [semgrep]

llm_hints:
  on_match: Semgrep invocation observed. --sarif output captured by the sarif attestor.
