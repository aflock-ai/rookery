# Detection-only catalog entry.
# Generated by scripts/gen-detection-catalog.py — do not edit by hand;
# edit the script and re-run.
apiVersion: cilock.detection/v0.1
name: drone-ci
detection_only: true

description: Drone — open-source container-native CI.

category: [build]

upstream:
  name: Drone
  source: "https://github.com/harness/drone"
  license: Apache-2.0 / commercial (Harness)
  vendor: "Harness, Inc."

pre:
  match:
    env_set: DRONE

llm_hints:
  on_match: "Running inside Drone CI. Build metadata available in DRONE_* env vars."
