# Detection-only catalog entry.
# Generated by scripts/gen-detection-catalog.py — do not edit by hand;
# edit the script and re-run.
apiVersion: cilock.detection/v0.1
name: hadolint
detection_only: true

description: "Hadolint — Dockerfile linter (pinned base images, install hygiene, USER root checks, 100+ rules)."

category: [lint]

upstream:
  name: Hadolint
  source: "https://github.com/hadolint/hadolint"
  license: GPL-3.0
  vendor: Hadolint maintainers

emits_formats: [sarif]

pre:
  match:
    argv_prefix: [hadolint]

llm_hints:
  on_match: "Hadolint invocation observed. `--no-fail --format sarif` output (routed via sh -c redirect) is captured by the sarif attestor."
