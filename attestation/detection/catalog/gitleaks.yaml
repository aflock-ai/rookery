# Detection-only catalog entry.
# Generated by scripts/gen-detection-catalog.py — do not edit by hand;
# edit the script and re-run.
apiVersion: cilock.detection/v0.1
name: gitleaks
detection_only: true

description: Gitleaks — secret detection in git history and source.

category: [secret-scan]

upstream:
  name: Gitleaks
  source: "https://github.com/gitleaks/gitleaks"
  license: MIT
  vendor: Gitleaks community

emits_formats: [sarif]

pre:
  match:
    argv_prefix: [gitleaks]

llm_hints:
  on_match: "Gitleaks scan observed. --report-format sarif|json captures findings."
