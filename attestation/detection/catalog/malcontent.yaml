# Detection-only catalog entry.
# Generated by scripts/gen-detection-catalog.py — do not edit by hand;
# edit the script and re-run.
apiVersion: cilock.detection/v0.1
name: malcontent
detection_only: true

description: "malcontent (`mal`) — flags supply-chain-attack capabilities and behaviors in binaries and source (Chainguard)."

category: [vulnerability-scan]

upstream:
  name: malcontent
  source: "https://github.com/chainguard-dev/malcontent"
  license: Apache-2.0
  vendor: Chainguard

pre:
  match:
    any_of:
      - argv_prefix: [mal, scan]
      - argv_prefix: [mal, analyze]
      - argv_prefix: [mal, diff]

llm_hints:
  on_match: "malcontent (mal) scan observed. JSON findings (-o <file>) captured as a product."
