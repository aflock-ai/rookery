# Detection-only catalog entry.
# Generated by scripts/gen-detection-catalog.py — do not edit by hand;
# edit the script and re-run.
apiVersion: cilock.detection/v0.1
name: codeql
detection_only: true

description: "CodeQL — GitHub's semantic static-analysis engine; `codeql database analyze` produces SARIF for 100+ query packs."

category: [vulnerability-scan]

upstream:
  name: CodeQL
  source: "https://codeql.github.com/"
  license: proprietary
  vendor: GitHub (Microsoft)

emits_formats: [sarif]

pre:
  match:
    argv_prefix: [codeql]

llm_hints:
  on_match: "CodeQL invocation observed. `database analyze --format=sarifv2.1.0` output is captured by the sarif attestor."
