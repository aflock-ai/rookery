# Detection-only catalog entry.
# Generated by scripts/gen-detection-catalog.py — do not edit by hand;
# edit the script and re-run.
apiVersion: cilock.detection/v0.1
name: cloudtrail
detection_only: true

description: AWS CloudTrail — AWS account audit log.

category: [posture-scan]

upstream:
  name: AWS CloudTrail
  source: "https://docs.aws.amazon.com/awscloudtrail/"
  license: commercial
  vendor: Amazon Web Services

pre:
  match:
    argv_prefix: [aws, cloudtrail]

llm_hints:
  on_match: AWS CloudTrail query observed. Captured audit-log slice.
