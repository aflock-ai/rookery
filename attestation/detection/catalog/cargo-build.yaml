# Detection-only catalog entry.
# Generated by scripts/gen-detection-catalog.py — do not edit by hand;
# edit the script and re-run.
apiVersion: cilock.detection/v0.1
name: cargo-build
detection_only: true

description: cargo build — Rust build orchestrator.

category: [build]

upstream:
  name: Cargo
  source: "https://github.com/rust-lang/cargo"
  license: Apache-2.0 OR MIT
  vendor: Rust Foundation

recommended_trace: full

pre:
  match:
    argv_prefix: [cargo, build]

llm_hints:
  on_match: cargo build observed. commandrun (with --tracing) captures opens/writes; Cargo.lock captured by lockfiles.
